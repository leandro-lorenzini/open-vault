doctype html
head
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0, shrink-to-fit=no')
  title Activate account
  link(rel='stylesheet' href='/assets/bootstrap.min.css')
  link(rel='stylesheet' href='/assets/style.css')
section.py-4.py-xl-5
  .container
    .row.mb-5
      .col-md-8.col-xl-6.text-center.mx-auto
        h2 Activate account
        p
          | Your are one step away from securely sharing your passwords, please set a password to activate your account.
    .row.d-flex.justify-content-center
      .col-md-10.col-xl-6
        if errorCode
          .alert.alert-danger(role='alert')
            if errorCode === 400
              | The data submitted is invalid:
              ul
                each item of messages
                  li= item.message

            if errorCode === 301
              | The activation link has already been used, it's expired or invalid.

            if errorCode === 500
              | A techinical error happened, please try again later.

        .card.mb-10
          .card-body.d-flex.flex-column
            form.text-center(method='post' disabled=(errorCode && errorCode == 301 ? true:false))
              .mb-3
                input.form-control(type='text' id='email' placeholder='Email' value=email disabled)
              .mb-3
                input.form-control(type='password' name='password' placeholder='Password' disabled=(errorCode && errorCode == 301 ? true:false))
              .mb-3
                input.form-control(type='password' name='confirmPassword' placeholder='Confirm Password' disabled=(errorCode && errorCode == 301 ? true:false))
              .mb-3
                button.btn.btn-primary.d-block.w-100(type='submit' disabled=(errorCode && errorCode == 301 ? true:false)) Activate
script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js')